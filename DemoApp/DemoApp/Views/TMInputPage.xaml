<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="DemoApp.TMInputPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Trimble.Modus.Components.Controls;assembly=Trimble.Modus.Components"
             Title="Input">
  <ScrollView>
    <VerticalStackLayout>

      <StackLayout Padding="10"
                   Spacing="10">

        <StackLayout Orientation="Horizontal">
          <Label Grid.Column="0"
                 Text="IsEnabled"
                 HorizontalOptions="Center"
                 VerticalOptions="Center" />
          <Switch Grid.Column="1"
                  x:Name="isEnabled"
                  IsToggled="{Binding IsEnabled}"
                  Toggled="IsEnabled_Toggled" />
          <Label Grid.Column="0"
                 Text="IsReadOnly"
                 HorizontalOptions="Center"
                 VerticalOptions="Center" />
          <Switch Grid.Column="1"
                  x:Name="isReadOnly"
                  IsToggled="{Binding IsReadOnly}"
                  Toggled="IsReadOnly_Toggled" />
        </StackLayout>

        <controls:TMInput  x:Name="modusInput"
                           Placeholder="Input"
                           IsEnabled="{Binding IsEnabled}"
                           IsReadOnly="{Binding IsReadOnly}" />

        <controls:TMInput  x:Name="modusInputWithTitle"
                           TitleText="Text Input"
                           IsEnabled="{Binding IsEnabled}"
                           IsReadOnly="{Binding IsReadOnly}"
                           Placeholder="Input" />

        <controls:TMInput x:Name="validationInput"
                          TitleText="Success/Error Input"
                          HelperText="Enter a valid email"
                          IsEnabled="{Binding IsEnabled}"
                          IsReadOnly="{Binding IsReadOnly}"
                          InputValidation="TMInputValidation"
                          Placeholder="Text Input" />

        <controls:TMInput x:Name="filled"
                          TitleText="Filled"
                          Placeholder="Text Input"
                          IsEnabled="{Binding IsEnabled}"
                          IsReadOnly="{Binding IsReadOnly}"
                          Text="Filled" />

        <controls:TMInput x:Name="rightIcon"
                          TitleText="Right Icon"
                          Placeholder="Text Input"
                          RightIconSource="password_eye.png"
                          IsEnabled="{Binding IsEnabled}"
                          IsReadOnly="{Binding IsReadOnly}"
                          RightIconCommand="{Binding ShowPasswordCommand}"
                          IsPassword="{Binding ShowPassword}" />

        <controls:TMInput x:Name="leftIcon"
                          TitleText="Left Icon"
                          LeftIconSource="s_icon.png"
                          IsEnabled="{Binding IsEnabled}"
                          IsReadOnly="{Binding IsReadOnly}"
                          Placeholder="Text Input" />

        <controls:TMInput x:Name="bothIcon"
                          TitleText="Both Icon"
                          LeftIconSource="user.png"
                          Placeholder="Text Input"
                          IsEnabled="{Binding IsEnabled}"
                          IsReadOnly="{Binding IsReadOnly}"
                          RightIconSource="cv.png" />

      </StackLayout>
    </VerticalStackLayout>
  </ScrollView>
</ContentPage>
